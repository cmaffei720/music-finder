<div class = "container">
    <div class="row">
        <div class="col">
            <div class="container artist-cols">
                <h2>Artist Results</h2>
                <ul>
                {{#each artist}}
                    <li class = "li-item">
                        <br>
                        {{this.artistName}}
                    </li>
                    <li class = "li-item">
                        <br>
                    <button type="submit" class="btn add-btn" data-id={{this.id}}>Add {{this.songName}}</button> 
                    </li>
                {{/each}}
                </ul>
            </div>
        </div>
    </div>
</div>